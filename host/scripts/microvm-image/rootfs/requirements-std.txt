# Pip-only importables that Debian bookworm does not package (or packages too old).
# Installed into the SYSTEM interpreter at image-build time via
# `pip install --break-system-packages` (deliberate; PEP 668) so that ad-hoc
# `python3 script.py` can import them without a runtime install. Runtime installs
# of anything NOT listed here still prefer uv (`uv run`, `uv pip`) per convention.
# Keep top-level requirements pinned. The build also writes
# rootfs-python.freeze with the full resolved environment.

fpdf2==2.8.3
markdownify==0.14.1
pdfplumber==0.11.5
duckdb==1.2.2
pyarrow==19.0.1
python-pptx==1.0.2
